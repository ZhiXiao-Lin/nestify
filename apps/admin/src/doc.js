/*
 * 此文件用于github pages，与业务无关
 */

import React from 'react';
import ReactDOM from 'react-dom';
import Root from 'layout/Root/DocRoot';

ReactDOM.render(
  <Root />,
  document.getElementById('app')
);
