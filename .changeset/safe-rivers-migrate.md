---
'@a3s-lab/migrations': patch
---

Validate migration configuration, add async NestJS registration and explicit runner execution, make custom name patterns deterministic, coalesce in-flight runs, and fail closed for every Kysely failure result.
