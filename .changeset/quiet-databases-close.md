---
'@a3s-lab/kysely': patch
---

Validate Kysely and PostgreSQL configuration, honor caller-owned instances, make owned connection shutdown idempotent, and bound SQL diagnostics with sensitive parameters and stacks disabled by default.
